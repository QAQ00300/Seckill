spring:
  application:
    name: SK-user
  cloud:
    compatibility-verifier:
      enabled: false

  datasource:
    url: jdbc:mysql://1Panel-mysql-JS9J:3306/seckill?useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: Ppg123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 3000
    lettuce:
      pool:
        max-active: 100
        max-wait: -1
        max-idle: 10
        min-idle: 5

seckill:
  cache:
    enabled: true
    type: redis

# MyBatis-Plus 配置
mybatis-plus:
  type-aliases-package: com.seckill.user.bo.eo
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 服务器配置
server:
  port: 8082

