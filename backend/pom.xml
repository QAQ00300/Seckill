<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.seckill</groupId>
    <artifactId>seckill-backend</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>
    <name>SecKill Backend</name>
    <description>SecKill Backend</description>
    <properties>
        <!-- 基本信息 -->
        <revision>release-1.0.0</revision>

        <!-- Java版本 -->
        <java.version>17</java.version>
        <maven.compiler.plugin>3.11.0</maven.compiler.plugin>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Spring相关版本 -->
        <spring-boot.version>3.5.12</spring-boot.version>
        <spring-boot-maven.version>3.5.12</spring-boot-maven.version>
        <spring-cloud.version>2025.0.1</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>

        <gateway-server.version>4.3.2</gateway-server.version>

        <!--其他-->
        <springfox.version>3.0.0</springfox.version>
        <jakarta.validation.version>3.0.2</jakarta.validation.version>
        <openfeign.version>4.0.6</openfeign.version>
        <loadbalancer.version>4.0.5</loadbalancer.version>

        <!-- alibaba相关版本 -->
        <transmittable-thread-local.version>2.8.1</transmittable-thread-local.version>
        <fastjson2.version>2.0.49</fastjson2.version>

        <!-- 数据库 -->
        <mysql.version>8.0.33</mysql.version>
        <mybatis-plus.version>3.5.15</mybatis-plus.version>
        <druid.version>1.2.18</druid.version>

        <!-- Redis -->
        <jedis.version>4.4.3</jedis.version>
        <redisson.version>3.23.2</redisson.version>

        <!-- 消息队列 -->
        <rocketmq.version>5.3.2</rocketmq.version>
        <kafka.version>3.4.1</kafka.version>

        <!-- 工具类 -->
        <lombok.version>1.18.30</lombok.version>
        <hutool.version>5.8.40</hutool.version>
        <guava.version>32.0.1-android</guava.version>
        <swagger.version>2.2.15</swagger.version>
        <resilience4j.version>2.1.0</resilience4j.version>

        <!-- 测试 -->
        <junit.version>5.10.0</junit.version>
        <jmeter.version>5.6.2</jmeter.version>

    </properties>

    <modules>
        <module>SK-gateway</module>
        <module>SK-user</module>
        <module>SK-order</module>
        <module>SK-core</module>
        <module>SK-common</module>
        <module>SK-client</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot依赖管理 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-actuator-autoconfigure</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-configuration-processor</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-web</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-gateway-server</artifactId>
                <version>${gateway-server.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-gateway-server-webflux</artifactId>
                <version>${gateway-server.version}</version>
            </dependency>

            <!-- 1. WebFlux - 提供响应式编程支持（必须保留） -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-webflux</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <!-- Spring Cloud依赖管理 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
            </dependency>

            <!-- Spring Cloud Alibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId>
                <version>${transmittable-thread-local.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>

            <!-- Sentinel Gateway 熔断降级 -->
            <!-- Resilience4j 熔断降级（Spring Cloud 官方推荐） -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-circuitbreaker-reactor-resilience4j</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>


            <!--工具-->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger.version}</version>
            </dependency>

            <!--其他依赖-->
            <dependency>
                <groupId>io.springfox</groupId>
                <artifactId>springfox-boot-starter</artifactId>
                <version>${springfox.version}</version>
            </dependency>

            <dependency>
                <groupId>jakarta.validation</groupId>
                <artifactId>jakarta.validation-api</artifactId>
                <version>${jakarta.validation.version}</version>
            </dependency>

            <!-- OpenFeign 远程调用 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-openfeign</artifactId>
                <version>${openfeign.version}</version>
            </dependency>

            <!-- Spring Cloud LoadBalancer -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-loadbalancer</artifactId>
                <version>${loadbalancer.version}</version>
            </dependency>


            <!-- ============ 第一阶段：基础微服务 ============ -->
            <!-- MySQL -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- MyBatis Plus - Spring Boot 3.x 兼容版本 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- 数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- ============ 第二阶段：Redis集成 ============ -->
            <!-- Spring Boot Redis Starter -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-data-redis</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>

            <!-- Redisson分布式锁 -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- Jedis客户端 -->
            <dependency>
                <groupId>redis.clients</groupId>
                <artifactId>jedis</artifactId>
                <version>${jedis.version}</version>
            </dependency>

            <!-- ============ 第三阶段：消息队列集成 ============ -->
            <!-- RocketMQ -->
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq.version}</version>
            </dependency>

            <!-- Kafka -->
            <dependency>
                <groupId>org.springframework.kafka</groupId>
                <artifactId>spring-kafka</artifactId>
                <version>${kafka.version}</version>
            </dependency>

            <!-- ============ 公共工具 ============ -->
            <!-- Lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <optional>true</optional>
            </dependency>

            <!-- Hutool工具包 -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- Guava -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>


            <!-- 模块注册(工具) -->
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>SK-client</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>SK-gateway</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>SK-common</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 模块注册(user) -->
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-ao</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-bo</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-api</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-biz</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-constant</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>user-boot</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 模块注册(order) -->
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-ao</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-bo</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-api</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-biz</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-constant</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>order-boot</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 模块注册(core) -->
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>core-boot</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>core-cache</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>core-mq</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>core-seckill</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.seckill</groupId>
                <artifactId>core-stock</artifactId>
                <version>${revision}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>





    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>1.5.0</version>
                    <executions>
                        <execution>
                            <id>flatten</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>flatten</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>flatten.clean</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot-maven.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
